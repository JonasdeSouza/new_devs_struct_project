<h1>Editar pedido</h1>
<h3>Produto: <%= @product.name %></h3>
<%= form_with model: @order do |form| %>
  <div class="field">
    <%= form.label :quantity, 'Quantidade' %>
    <%= form.select :quantity, options_for_select(1..@product.quantity) %>
  </div>

  <div class="actions">
    <%= form.submit "Atualizar pedido" %>
  </div>
<% end %>

<%= link_to 'Voltar', orders_path %>